<div class="span8">
<div class="page-header">
  <h1>Search : <%= params[:jobq] %></h1>
</div>

<% if !@jobs.blank? %>
<% i =0  %>
  <table class="table table-bordered table-striped table-hover" style="font-size:16px;">
    <thead>
      <tr>
        <th>#</th>
        <th colspan="3">Job at Company in Location</th>
      </tr>
    </thead>
    <tbody>
       <% @jobs.each do |job |%>
       <% i = i+1 %>
      <tr>
        <td><%= i %></td>
        <td colspan="3">
      <a href="<%=job_path(job)%>"><%= job.title %></a>
      <span class="lighterSmall" style="color: #93E08B;"> at </span><%= job.company %>
      <span class="lighterSmall" style="color: #93E08B;"> in </span><%= job.location %>
      </td>
      </tr>
        <% end %>
    </tbody>
  </table>
<% else %>
<div class="info">Nothing to be found for your searched criteria.</div>
<ul>
  <li>Look for typos in the searched word(s) and, if found, correct them.</li>
  <li>Be less specific. Try "web designer" instead of "senior web designer".</li>
  <li>Try similar words. If "designer" doesn\'t return anything, try "design".</li>
</ul>
<% end %>
</div>